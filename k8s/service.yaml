apiVersion: v1
kind: Service
metadata:
  name: sentiment-api-service
  labels:
    app: sentiment-api
spec:
  type: NodePort
  selector:
    app: sentiment-api
    slot: blue
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 32500
